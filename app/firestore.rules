rules_version = '2';
// DEMO ONLY: fully open, no auth (per PRD — access control is post-MVP).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
